name: Build and Publish

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    name: Test on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Run tests
        run: dotnet test test/ --no-build --configuration Release --verbosity normal

  publish_nuget:
    name: Publish NuGet Package
    runs-on: ubuntu-latest
    needs: [test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Calculate version
        id: version
        run: |
          MAJOR_VERSION=$(cat VERSION)
          MINOR_VERSION=${{ github.run_number }}
          APP_VERSION="${MAJOR_VERSION}.${MINOR_VERSION}.0"
          echo "APP_VERSION=$APP_VERSION" >> $GITHUB_ENV
          echo "version=$APP_VERSION" >> $GITHUB_OUTPUT
          echo "Calculated version: $APP_VERSION (run number: ${{ github.run_number }})"

      - name: Generate changelog
        id: changelog
        run: |
          git fetch --tags --force
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          
          if [ -z "$LAST_TAG" ]; then
            echo "No previous tag found, using all commits"
            CHANGELOG=$(git log --pretty=format:'- %h %s (%an) [%ad]' --date=iso | head -20)
          else
            echo "Previous tag: $LAST_TAG"
            CHANGELOG=$(git log ${LAST_TAG}..HEAD --pretty=format:'- %h %s (%an) [%ad]' --date=iso)
          fi
          
          echo "last_tag=${LAST_TAG:-initial}" >> $GITHUB_OUTPUT
          
          # Save changelog to environment file properly
          echo "changelog<<CHANGELOG_EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "CHANGELOG_EOF" >> $GITHUB_OUTPUT

      - name: Build package
        run: |
          mkdir -p ./nupkg
          dotnet pack src/Exec.csproj \
            -c Release \
            --include-source \
            --include-symbols \
            --output ./nupkg \
            -p:Version=${{ env.APP_VERSION }} \
            -p:PackageVersion=${{ env.APP_VERSION }}
          ls -la ./nupkg

      - name: Publish to NuGet
        if: env.APP_VERSION != ''
        shell: pwsh
        run: |
          $PkgPath = "./nupkg/Exec.${{ env.APP_VERSION }}.nupkg"
          if (!(Test-Path $PkgPath)) {
            Write-Error "No package found at $PkgPath"
            Write-Host "Contents of ./nupkg:"
            Get-ChildItem ./nupkg
            exit 1
          }

          $ApiKey = "${{ secrets.NUGET_API_KEY }}"
          if (!$ApiKey) {
            Write-Error "NuGet API Key not found in secrets"
            exit 1
          }

          Write-Host "Publishing package: $PkgPath"
          dotnet nuget push $PkgPath -k $ApiKey -s https://api.nuget.org/v3/index.json --skip-duplicate
          Write-Host "Package published successfully!"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.APP_VERSION }}
          name: v${{ env.APP_VERSION }}
          body: |
            Release v${{ env.APP_VERSION }} for Exec.

            ## Changes
            
            ${{ steps.changelog.outputs.changelog }}
          token: ${{ secrets.GITHUB_TOKEN }}